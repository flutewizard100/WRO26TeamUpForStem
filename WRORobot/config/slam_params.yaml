slam_toolbox:
  ros__parameters:
    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_linear_search_factor: 0.5

    minimum_travel_distance: 0.1     # 10 cm — was 0.5 by default
    minimum_travel_heading: 0.15     # ~9 deg — was 0.5 rad
    map_update_interval: 3.0
    resolution: 0.1
    max_laser_range: 3.0             # match LD19 range_max
    throttle_scans: 3                # process every 3rd scan — reduces CPU
    minimum_time_interval: 1.0       # min seconds between processed scans


    qos_overrides:
      /scan:
        subscription:
          reliability: best_effort
